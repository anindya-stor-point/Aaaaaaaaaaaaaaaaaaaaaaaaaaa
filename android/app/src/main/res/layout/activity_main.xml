<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="OTG Screen Mirror"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginBottom="32dp"/>

    <Button
        android:id="@+id/btnStartSender"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Started as Sender (Share Screen)"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/btnStartReceiver"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Start as Receiver (View Screen)" />
        
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Instructions: Connect both phones with an OTG cable via USB. First start the Receiver on the Host phone. Then start the Sender on the phone you want to mirror."
        android:textColor="#666666"
        android:layout_marginTop="32dp"
        android:gravity="center" />

</LinearLayout>
